[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && cd frontend && npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
